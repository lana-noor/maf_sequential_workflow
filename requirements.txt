# Core agent framework
agent-framework --pre

# Word document generation (executor step)
python-docx

# Pydantic for structured output models
pydantic>=2.0

python-dotenv

openai

# FastAPI backend + SSE (for the React UI)
fastapi>=0.115
uvicorn[standard]>=0.30